from app import db


class Friendship(db.Model):
    """Друзья"""
    # user_id = db.Column(db.Integer) (Foreign Key)
    # friend_id = db.Column(db.Integer) (Foreign Key)
    # connection_date = db.Column(db.Date)
    # status = db.Column(db.Integer) (e.g., pending, accepted, blocked)

